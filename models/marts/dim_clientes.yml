version: 2

models:
  - name: dim_clientes
    description: Dimensão de clientes da empresa.
    columns:
      - name: id_cliente
        description: Chave primária dos clientes
        tests:
          - unique
          - not_null